function [name] = id2name(pths, id)

	[allNames, allIds] = textread(paths.masterFile, '%s \t %d\n');
	[~,ia,ib] = intersect(id, allIds, 'stable');
	assert(length(ia)==length(id));
	name = allNames(ib);
	
	fclose(fid);
end
